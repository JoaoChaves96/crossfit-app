/**
 * Smoke test — verifies the Jest + jest-expo setup is working.
 * This test contains no application logic.
 */
describe('Jest setup', () => {
  it('runs a trivial assertion', () => {
    expect(true).toBe(true);
  });
});
