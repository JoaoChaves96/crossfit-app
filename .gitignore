**/node_modules/*

**/.claude/worktrees/*

/.DS_Store

.agents/

.claude/

skills-lock.json

# Impeccable local review artifacts (screenshots + detector cache).
# The DESIGN sidecar (frontend/.impeccable/design.json) IS tracked.
.design-review/
/.impeccable/
**/.impeccable/shots/
**/.impeccable/hook.cache.json
# Playwright artefacts, if a run is started from the repo root rather than frontend/
test-results/
playwright-report/

# Throwaway screenshots from live verification passes (browser-driven card sign-off)
.verify-shots/

# Local-only research notes. Never tracked.
.research/
